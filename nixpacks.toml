[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["cd backend && npm install --include=dev"]

[phases.build]
cmds = ["cd backend && npm run build"]

[start]
cmd = "node backend/dist/index.js"
