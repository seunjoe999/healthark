{
  "name": "compcarehub",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "build": "npm run build --prefix frontend && npm run build --prefix backend",
    "start": "node backend/dist/index.js",
  },
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "node-ssh": "^13.2.1",
    "pg": "^8.21.0"
  }
}
